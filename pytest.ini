[pytest]
testpaths = tests
pythonpath = .
# A GUI suite can hang on an unattended modal dialog, so cap every test.
# The thread method is required on Windows, where SIGALRM is unavailable.
addopts = -q --timeout=60 --timeout-method=thread
